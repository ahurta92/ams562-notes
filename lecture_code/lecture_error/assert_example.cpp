#include <cassert>

double sqrt_root(double x) {

        assert(rea
}

int main() {
  int x = 8;
  int y = -1;
  try {

    int z = area(x, y);
  } catch (Bad_area) {
    cerr << "opps! Bad area calcluation - fix programming" << endl;
  }
}
